package com.voxelgame.render;

/**
 * First-person camera. Manages position, rotation (yaw/pitch),
 * and computes the view and projection matrices each frame.
 */
public class Camera {
    // TODO: position, rotation, view matrix, projection matrix, FOV
}
