package com.voxelgame.render;

/**
 * Loads and manages the block texture atlas. Provides UV coordinate
 * lookups for each block face based on texture index.
 */
public class TextureAtlas {
    // TODO: load atlas.png via STB, compute UV regions, bind texture
}
