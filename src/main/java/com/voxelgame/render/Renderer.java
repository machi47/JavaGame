package com.voxelgame.render;

/**
 * Main rendering coordinator. Manages the render pipeline:
 * sets up frame, renders opaque chunks, transparent pass, water, HUD.
 */
public class Renderer {
    // TODO: frame setup, chunk sorting, draw calls, cleanup
}
