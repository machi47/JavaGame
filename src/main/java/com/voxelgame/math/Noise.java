package com.voxelgame.math;

/**
 * Base interface/class for noise generators.
 * Defines the contract for 2D and 3D noise sampling.
 */
public abstract class Noise {
    // TODO: abstract eval2D(x, z), eval3D(x, y, z)
}
