package com.voxelgame.save;

/**
 * Region-based chunk storage (similar to Minecraft's .mca format).
 * Stores multiple chunks in a single file with an offset table header.
 */
public class RegionFile {
    // TODO: header, offset table, read/write chunk data, compaction
}
