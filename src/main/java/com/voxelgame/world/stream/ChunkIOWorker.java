package com.voxelgame.world.stream;

/**
 * Background worker for chunk I/O (load/save). Reads and writes
 * chunk data to region files on disk.
 */
public class ChunkIOWorker implements Runnable {
    @Override
    public void run() {
        // TODO: serialize/deserialize chunks, region file I/O
    }
}
