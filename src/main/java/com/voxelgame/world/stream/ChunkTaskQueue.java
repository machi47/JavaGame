package com.voxelgame.world.stream;

/**
 * Priority queue for chunk tasks. Orders work by distance to player
 * and task type priority (generation > meshing > saving).
 */
public class ChunkTaskQueue {
    // TODO: priority queue, enqueue, dequeue, cancel, clear
}
