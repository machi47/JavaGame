package com.voxelgame.world.stream;

/**
 * Manages chunk lifecycle: loading, generation, meshing, and unloading.
 * Coordinates background workers and maintains the load radius around the player.
 */
public class ChunkManager {
    // TODO: load radius, chunk queue, worker coordination, tick
}
