package com.voxelgame.world.stream;

/**
 * Defines chunk task prioritization strategy. Computes priority scores
 * based on distance to player, direction of movement, and task type.
 */
public class PriorityPolicy {
    // TODO: priority calculation, distance weighting, direction bias
}
