package com.voxelgame.world.mesh;

/**
 * GPU-side mesh data for a chunk. Holds VAO/VBO handles, vertex count,
 * and manages upload/disposal of vertex data.
 */
public class ChunkMesh {
    // TODO: VAO, VBO, vertex count, upload(), draw(), dispose()
}
