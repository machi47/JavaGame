package com.voxelgame.world.mesh;

/**
 * Abstract meshing interface. Defines the contract for converting
 * chunk block data into renderable vertex data.
 */
public interface Mesher {
    // TODO: mesh(chunk, neighbors) -> ChunkMesh
}
